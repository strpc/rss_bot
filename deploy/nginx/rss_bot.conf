server {
    listen              443 ssl;
    server_name         a9740923c3ed.sn.mynetname.net;

    ssl_certificate     /home/ubuntu/.ssh/cert.pem;
    ssl_certificate_key /home/ubuntu/.ssh/private.key;
    access_log  /var/log/nginx/django_rss_bot_nginx.log;

    location /SUPER_TOKEN {
        proxy_pass http://127.0.0.1:10000;
    }

    location /static/ {
        root /super_dir/src/;
        expires 30d;
    }
}

server {
    listen              10001;
    server_name         192.168.88.11;
    access_log  /var/log/nginx/django_rss_bot_nginx.log;

    location / {
        proxy_pass http://127.0.0.1:10000;
    }

    location /static/ {
        root /super_dir/src/;
        expires 30d;
    }
}
